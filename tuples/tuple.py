#A tuple is a collection which is ordered and unchangeable. It allows duplcate mebers
city = ('jaffna','colombo','galle','trincomalee')
print(city)

#printing length
print(len(city))

#tuple constrctor method
thistuple = tuple(("joy","roy","bejoy"))
print(thistuple)